# Main Page

The home page at `/` lives in the `(main)` route group with a shared Footer layout.

## Converter Sub-page

Generally doesn't have to be made better or changed. Mostly there as a bare-boned converter and redirecting to Paper's site.

## Assets

- `public/assets/images/` — `home-bg.webp`, `console.png`, `github.png`, `Paper.png`
- `public/assets/ico/` — Favicons
